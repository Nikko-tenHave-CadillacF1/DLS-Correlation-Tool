# ─── Core (required) ─────────────────────────────────────────────────────────
pandas>=1.3.0
numpy>=1.21.0           # `np.trapezoid` is used when available (NumPy 2.0+),
                        # with a `np.trapz` fallback for older versions.
matplotlib>=3.5.0
scipy>=1.7.0            # `scipy.stats.theilslopes` is used for robust fits.

# ─── Optional ────────────────────────────────────────────────────────────────
# Parquet input: at least one engine is required when reading .parquet files.
pyarrow>=15.0.0;       python_version < "3.14"
fastparquet>=2024.11.0; python_version < "3.14"

# PowerPoint export (Windows only). Required only when EXPORT_TO_POWERPOINT=True.
pywin32>=306; sys_platform == "win32"

# Progress bars during plot generation. The tool runs without it.
tqdm>=4.64.0
