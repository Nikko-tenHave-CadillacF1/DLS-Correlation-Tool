# ─── Core (required) ─────────────────────────────────────────────────────────
# Upper bounds are broad — they exist to catch known major-version API
# breaks (e.g. numpy 2.0 removed np.trapz; a future numpy 3.0 or pandas 3.0
# would need retesting).
pandas>=1.3.0,<3.0
numpy>=1.21.0,<3.0       # `np.trapezoid` is used when available (NumPy 2.0+),
                         # with a `np.trapz` fallback for older versions.
matplotlib>=3.5.0,<4.0
scipy>=1.7.0,<2.0        # `scipy.stats.theilslopes` is used for robust fits.

# ─── Optional ────────────────────────────────────────────────────────────────
# Parquet input: at least one engine is required when reading .parquet files.
# Both have wheels for Python 3.14 as of late-2025; if your platform lacks one,
# the other will be auto-selected at runtime.
pyarrow>=15.0.0,<22.0
fastparquet>=2024.11.0,<2027.0

# PowerPoint export. python-pptx is cross-platform (preferred); pywin32 is
# only used as a fallback on Windows when python-pptx isn't available.
python-pptx>=0.6.23,<2.0
pywin32>=306; sys_platform == "win32"

# Progress bars during plot generation. The tool runs without it.
tqdm>=4.64.0,<5.0
