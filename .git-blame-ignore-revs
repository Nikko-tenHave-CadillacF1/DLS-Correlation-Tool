# Commits listed here are ignored by `git blame` so wholesale mechanical
# reformats don't obscure real authorship.
#
# Enable locally with:
#     git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# One SHA per line. Add a comment on the line above each entry so future
# maintainers know what the ignored commit did.

# style: apply ruff format across codebase (no semantic changes)
# ← Append the commit SHA on the line below AFTER creating the commit.
#   Suggested command:
#     git commit -am "style: apply ruff format across codebase (no semantic changes)"
#     git rev-parse HEAD | ForEach-Object { Add-Content .git-blame-ignore-revs $_ }
#     git commit -am "chore: register ruff-format commit in .git-blame-ignore-revs"
